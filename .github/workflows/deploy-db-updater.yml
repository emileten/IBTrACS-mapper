name: Deploy DB Updater

# Manual deployment only - trigger via GitHub Actions UI
on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy/Update DB Updater
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./db-updater
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run updater
        env:
          # TODO: Add required environment variables
          # SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          # SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python updater.py
      
      - name: Deploy
        # TODO: Add deployment steps (e.g., update Cloud Scheduler job, Cloud Function, etc.)
        run: |
          echo "Deploying db-updater..."
          # Add your deployment commands here

