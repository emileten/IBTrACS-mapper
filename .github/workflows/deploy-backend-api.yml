name: Deploy Backend API

# Manual deployment only - trigger via GitHub Actions UI
on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Backend API to Production
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend-api
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run tests
        run: |
          pip install -r requirements-dev.txt
          pytest || echo "Tests not fully configured"
      
      - name: Deploy
        # TODO: Add deployment steps (e.g., deploy to Cloud Run, AWS Lambda, etc.)
        run: |
          echo "Deploying backend API..."
          # Add your deployment commands here
          # Example: gcloud run deploy ibtracs-api --source .

